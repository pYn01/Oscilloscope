<Window x:Class="OscilloscopeUI.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="SpeaLite Oscilloscope" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding VoltageText}" FontSize="30" HorizontalAlignment="Center" Margin="10" />

        <lvc:CartesianChart Grid.Row="1" Series="{Binding VoltageGraph}" Margin="20" DisableAnimations="True" />
    </Grid>
</Window>